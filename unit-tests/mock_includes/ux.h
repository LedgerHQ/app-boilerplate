#pragma once

// Mock ux.h for unit tests
// This file provides stubs for UX-related functions that are not needed for unit tests

#define UX_INIT() do {} while(0)
#define UX_DISPLAY(x,y,z) do {} while(0)
