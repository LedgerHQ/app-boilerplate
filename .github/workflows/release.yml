name: GitHub release
on:
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  github-release:
    name: GitHub release using the reusable workflow
    uses: LedgerHQ/ledger-app-workflows/.github/workflows/reusable_app_release.yml@mbr/release-check-version
    if: startsWith(github.ref, 'refs/tags/')
    with:
      app_ref_name: ${{ github.ref_name }}