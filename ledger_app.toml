[info]
version = 2

[app]
build_directory = "./"
sdk = "C"
devices = ["nanox", "nanos+", "stax", "flex"]

[unit_tests]
directory = "./unit-tests/"

[pytest.standalone]
directory = "./tests/"

[pytest.swap]
directory = "./tests_swap/"
